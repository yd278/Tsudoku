#ifndef LOCKEDCANDIDATES_H
#define LOCKEDCANDIDATES_H


#include <utility>
#include <string>
#include <Grid.h>
std::pair<bool,std::string> findLockedCandidates(const Grid &grid);

#endif // LOCKEDCANDIDATES_H